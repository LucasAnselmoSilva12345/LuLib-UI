import React from 'react';
import ReactDOM from 'react-dom/client';
import { ImportsComponents } from './components/ImportsComponents.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ImportsComponents />
  </React.StrictMode>
);